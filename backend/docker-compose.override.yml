services:
  api:
    ports:
      - "5000:8080"
    volumes:
      - ./src/InstaClone.Api:/src
    environment:
      - ASPNETCORE_URLS=http://+:8080

  db:
    ports:
      - "1433:1433"
    volumes:
      - sqldata:/var/opt/mssql
